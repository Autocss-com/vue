import { createApp } from 'vue'
// import './style.css' // native styles disabled — demo is styled solely by the remote AutoCSS stylesheets (see index.html)
import App from './App.vue'

createApp(App).mount('app-container')
